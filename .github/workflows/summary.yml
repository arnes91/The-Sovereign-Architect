name: "PR Summary"
on:
  pull_request:
    types: [opened, synchronize]

jobs:
  summary:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Generate Summary
        run: echo "Summary generated for PR #${{ github.event.pull_request.number }}"
